apiVersion: v1
kind: Service
metadata:
  name: odooapp
  namespace: odoo
spec:
  #type: NodePort
  selector:
    app: odooapp
  ports:
    - name: http
      protocol: TCP
      port: 8069  
      targetPort: 8069
    #  nodePort: 30069